<%= form_with(model: tweet, local: true) do |form| %>
  <div class= "form-group">
    <%= form.text_area :content, row: 5, class: 'form-control', id: 'tweet_content' %>
  </div>

  <%= form.submit nil, class: 'btn btn-primary' %>

<% end %>